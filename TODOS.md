# TODOS

Generated by /plan-eng-review on 2026-06-02
Updated after implementation on 2026-06-02

## Completed (Phase 1 收尾)

- [x] **T1 — 增量更新机制** (P1)
  - 基于文件 hash/mtime 的增量检测，替代 `clearProject()` 全量重建
  - 文件：`src/cli/actions.ts` (原 `src/index.ts`)
  - 验证：73 页面 yadea-wiki 增量导入 < 1s

- [x] **T2 — 补全 builder 测试** (P1)
  - 覆盖 key-value 格式、flat 格式、API 提取、关系构建（pageHasApis/fieldCallsApis）
  - 文件：`tests/graph/builder.test.ts`

- [x] **T3 — 补全 analyze-impact 测试** (P2)
  - 覆盖 apiName / fieldName / pageId 三个分支
  - 文件：`tests/mcp/tools/analyze-impact.test.ts`

- [x] **T4 — 补全 extractAPIReferences 测试** (P2)
  - 覆盖正常匹配、边界情况（非 Api 后缀、大写开头）
  - 文件：`tests/parser.test.ts`

- [x] **T5 — Store db 私有化 + 接口重构** (P2)
  - 将 `db` 从 `Store` 接口移除，使用 `WeakMap` + `getStoreDatabase()` 内部访问
  - 所有 `store.db` 调用迁移到 `getStoreDatabase(store)`
  - 文件：`src/store/sqlite.ts`, `src/mcp/tools/*.ts`, `tests/**/*.test.ts`

- [x] **T6 — 消除 builder DRY 违反 + 类型安全** (P3)
  - 提取 `parseAreaContent<T extends WithId>` 通用解析器，消除重复分支
  - 添加 `WithId` 泛型约束，移除 `as Record<string, unknown>` 类型绕过
  - 添加工厂函数 `createFieldOptions` / `createGridColumnOptions` / `createButtonOptions`
  - 文件：`src/graph/builder.ts`

## 额外完成项

- [x] **validate CLI 统一非零退出码**
  - `validate` 命令在 consistency 或 mapping 存在问题时统一 `process.exit(1)`
  - 文件：`src/cli/actions.ts`, `src/index.ts`

- [x] **code-scanner 渐进增强：正则 + AST 降级**
  - `scanRoutes` / `scanSchema` / `scanServices` 均采用"正则快速路径 + TypeScript AST fallback"
  - AST 路径使用动态 `import('typescript')`，不增加启动依赖
  - `buildMapping` / `scanPageDir` 改为 `async` 以支持 AST fallback
  - 文件：`src/code-scanner.ts`

- [x] **补齐 0% 覆盖率文件测试**
  - `src/mcp/server.ts` → `tests/mcp/server.test.ts`（使用 `InMemoryTransport` + `Client`）
  - `src/index.ts` → 提取 `src/cli/actions.ts` + `tests/cli-actions.test.ts`
  - 新增 CLI E2E 测试：`tests/cli.test.ts`（ingest / query / map）
  - 总测试数：73 → 113，覆盖率：73.65% → 84.96%

- [x] **buildGraphAsync 并发限制**
  - 添加零依赖 `withConcurrency<T, R>` worker pool 实现（默认并发 50）
  - 重构 `buildGraphAsync` 为不可变收集模式（return sub-arrays then flat）
  - 文件：`src/graph/builder.ts`

## Deferred (Phase 2)

- [ ] **check_consistency MCP 工具扩展** (P1, deferred)
  - 检测：缺失 main.md 的页面、空字段列表、孤儿 API、字段名与 API 名不匹配
  - 文件：`src/mcp/tools/check-consistency.ts`
  - Deferred to: Phase 2（增量更新完成后）

- [ ] **Schema migration 框架** (P2, deferred)
  - 当前 `__version` 表只有硬编码版本 1，未来加表时需要升级机制
  - Deferred to: Phase 2

- [ ] **Semantic Mapper 深化** (P0, deferred)
  - 代码到功能清单的自动映射，design doc 中最高风险项
  - AST fallback 已落地，下一步：处理复杂路由格式、多文件 services/schema
  - Deferred to: Phase 2（跨 repo 验证）

- [ ] **custom.yml 插件接口** (P2, deferred)
  - 支持非标准页面的自定义解析器
  - Deferred to: Phase 2
