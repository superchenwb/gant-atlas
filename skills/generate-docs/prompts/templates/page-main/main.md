# 页面名称：{页面中文名}

---

## 一、页面概述

| 项目 | 说明 |
|------|------|
| 页面英文名 | `{page-name}` |
| 页面功能 | {一句话说明页面核心用途} |
| 页面类型 | `主页面(列表页)` / `主页面(Tabs 多区域)` |
| 所属模块 | `{模块名}` |
| 路由地址 | `{route}` |
| 进入页面后的默认展示 | {默认选中的 Tab、默认列表状态、默认说明区等} |

---

## 二、页面结构与文档拆分

> 本节先输出页面结构图，再列出文档拆分清单。

### 2.1 页面结构

```text
┌─────────────────────────────────────────────────────────────┐
│  查询区（SearchForm）                                       │
│  - {查询字段1，如：产品（必填）}                             │
│  - {查询字段2，如：配置表版本（必填）}                       │
│  - {查询字段3，如：变型产品（必填）}                         │
├─────────────────────────────────────────────────────────────┤
│  Tab / 主内容区（TabsButton / Grid / 其他容器）             │
│  [Tab1] [Tab2] [Tab3]                                      │
│                                                             │
│  按钮区（左）   |   按钮区（右）                            │
│                                                             │
│  Grid 表格区 / 主内容区                                     │
│                                                             │
│  {如有其他模块：树区 / 汇总区 / 附件区 / 说明区}             │
└─────────────────────────────────────────────────────────────┘
```

要求：

- 结构图必须反映页面真实层级。
- 如果是单 Grid 页面，可以不画 Tab 行，但要保留查询区、按钮区、主内容区。
- 如果是多 Tab 页面，必须把页签名称列出来。
- 如果页面没有左右按钮分区，按真实布局描述。

### 2.2 文档拆分清单

| 区域 | 页面内容 | 对应文档 |
|------|----------|----------|
| 查询区域 | {查询区域说明} | [search-area.md](./search-area.md) |
| 页面级按钮 | {页面级按钮说明；无则写“无独立页面级按钮”} | [button-area.md](./button-area.md) |
| 主表格 / 主内容区 | {单 Grid 页面说明} | [grid-area.md](./grid-area.md) |
| 其他可见模块 | {附件区、汇总区、树区等；无则写“无”} | [other-features.md](./other-features.md) |
| Tab / 区域 1 | {如：特征清单} | [tab-{name}/main.md](./tab-{name}/main.md) / [grid-area1.md](./grid-area1.md) |
| Tab / 区域 2 | {如：规则定义} | [tab-{name}/main.md](./tab-{name}/main.md) / [grid-area2.md](./grid-area2.md) |

> 多 Tab / 多 Grid 页面必须按区域拆文档；不要只保留一个总 `grid-area.md`。

### 2.3 全页面功能索引

| 功能名称 | 触发入口 / 载体 | 所属区域 | 关键条件 | 对应文档 |
|----------|-----------------|----------|----------|----------|
| {如：查询} | {查询按钮 / 自动触发} | 查询区 | {条件} | [search-area.md](./search-area.md) |
| {如：关联特征} | {按钮文案} | 页面级按钮 / Tab 按钮 | {条件} | [button-area.md](./button-area.md) / [button-{name}.md](./button-{name}.md) |
| {如：特征清单维护} | {Tab 名称 / Grid} | 主内容区 / Tab | {条件} | [grid-area.md](./grid-area.md) / 对应 Tab Grid 文档 |
| {如：附件维护} | {模块入口} | 其他模块 | {条件} | [other-features.md](./other-features.md) |

要求：

- 页面上每一个具体功能都要在这里出现一次。
- 复杂按钮优先链接到对应 `button-{name}.md`。
- 如果某个功能由独立模块承载，也要列出该模块入口。

---

## 三、页面主流程

| 步骤 | 用户操作 | 页面结果 |
|------|----------|----------|
| 1 | {如：选择查询条件} | {结果} |
| 2 | {如：点击查询} | {结果} |
| 3 | {如：切换 Tab / 编辑行 / 点击按钮} | {结果} |
| 4 | {如：保存 / 删除 / 打开弹窗} | {结果} |

---

## 四、页面级接口链路

### 4.1 页面初始化 / 查询链路

| 类型 | 接口 | 用途 | 触发时机 |
|------|------|------|----------|
| 初始化接口 | `{METHOD} {URL}` / `无` | {用途} | 页面加载时 |
| 主查询接口 | `{METHOD} {URL}` | {用途} | 点击查询 / 默认加载 / 切换 Tab |

### 4.2 其他接口导航

| 场景 | 说明 | 对应文档 |
|------|------|----------|
| 页面级按钮接口 | {简述} | [button-area.md](./button-area.md) |
| Grid 行操作接口 | {简述} | [grid-area.md](./grid-area.md) / 对应 Tab Grid 文档 |
| 弹窗提交接口 | {简述} | [popup-{name}.md](./popup-{name}.md) / 对应 Tab 文档 |

---

## 五、特殊说明

- {如有页面级限制、业务状态限制、只读态限制}
- {如页面存在多个入口导致默认展示不同}
