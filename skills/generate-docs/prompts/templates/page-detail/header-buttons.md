# 头部按钮

---

## 一、按钮概述

| 项目 | 说明 |
|------|------|
| 按钮区域位置 | 详情头部 |
| 是否随状态变化 | `是` / `否` |
| 是否存在复杂按钮功能文档 | `是` / `否` |

> 不能只写头部按钮概述。每一个头部按钮都要逐项描述完整；复杂按钮优先拆成 `button-{name}.md`。

---

## 二、按钮清单

| 序号 | 按钮名称 | 显示条件 | 禁用条件 | 点击结果 | 二次确认 | 对应文档 | 说明 |
|------|----------|----------|----------|----------|----------|----------|------|
| 1 | {按钮名称} | {条件} | {条件} | {结果} | {确认文案或无} | [button-{name}.md](./button-{name}.md) / 当前文档 | {说明} |

要求：

- 一个按钮一行，不能合并写“编辑/发布/审批”。
- 如果按钮功能复杂，必须拆成 `button-{name}.md`，这里保留总清单和链接。

---

## 三、按钮行为与接口

| 操作名称 | 触发条件 | 接口 | 成功后结果 | 失败后结果 | 刷新范围 | 说明 |
|----------|----------|------|------------|------------|----------|------|
| {操作名称} | {条件} | `{METHOD} {URL}` | {结果} | {结果} | {整个详情 / 当前页签 / 返回列表} | {说明} |

---

## 四、状态差异矩阵

| 按钮名称 | 页面状态 / 权限状态 / 审批状态 | 可见性 | 可用性 | 点击后结果 |
|----------|---------------------------------|--------|--------|------------|
| {按钮名称} | {如：草稿 / 已发布 / 待审批 / 无权限} | {显示 / 隐藏} | {可用 / 禁用} | {结果} |

---

## 五、关联弹窗或按钮功能文档

| 功能名称 | 触发入口 | 用途 | 详细文档 |
|----------|----------|------|----------|
| {按钮名称或弹窗名称} | {入口} | {用途} | [button-{name}.md](./button-{name}.md) / [popup-{name}.md](./popup-{name}.md) |

要求：

- 如果弹窗由某个复杂按钮独占触发，优先写进 `button-{name}.md`。
- 只有复用型或必须独立表达的弹窗才单独保留 `popup-{name}.md`。

---

## 六、特殊说明

- {如同一按钮在不同状态下文案不同}
- {如按钮依赖审批状态或权限}
