# {{methodName}}

> 类型：{{Hook / 工具函数}}
> 所属业务领域：{{businessDomain}}

## 功能描述

{{一句话说明该方法的核心作用，例如："封装 BOM 主页面的数据查询逻辑，管理查询参数、加载状态和结果数据。"}}

---

## 参数

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| {{param1}} | {{类型描述}} | 是 | {{说明}} |
| {{param2}} | {{类型描述}} | 否 | {{说明}} |

<!--
无参数时写：该方法不接收参数。
-->

---

## 返回值

| 字段名 | 类型 | 说明 |
|---|---|---|
| {{field1}} | {{类型描述}} | {{说明}} |
| {{field2}} | {{类型描述（函数）}} | {{说明，如"触发查询，传入查询条件对象"}} |
| {{field3}} | {{布尔值}} | {{说明，如"是否处于加载中状态"}} |

<!--
如果返回单一值（非解构对象），则改为：

## 返回值

返回 {{类型描述}}，表示 {{含义}}。
-->

---

## 核心功能

### {{功能点1，如：数据查询}}

{{描述这个功能点的触发时机和可观察的行为结果。
例如："调用返回的 onSearch 函数后，使用传入的查询条件发起数据请求，更新返回的数据列表和加载状态。查询完成后自动将表格滚动到第一行。"}}

### {{功能点2，如：翻页管理}}

{{描述该功能点。
例如："返回的分页信息随查询结果同步更新，调用 onPageChange 后使用新的页码重新发起查询。"}}

### {{功能点3，如：状态管理}}

{{描述该功能点。
例如："返回的 loading 在请求发起时变为 true，请求结束（无论成功失败）后恢复为 false。"}}

<!--
常见功能点：
- 数据查询 / 数据加载
- 翻页 / 排序管理
- 状态重置
- 表单提交
- 数据保存
- 权限判断
- 数据格式转换（工具函数场景）
- 错误处理
-->

---

## 使用场景

{{描述该方法适合在哪类页面或组件中使用，以及与其他方法配合使用的关系。
例如：
- 配合 SearchForm 和 Grid 在主页面中使用
- 查询条件由 SearchForm 的 onSearch 回调提供
- 返回的 dataSource 直接传入 Grid 的 dataSource 属性
}}

**适用条件**：

{{是否有使用前提，例如：
- 需要在 BOM 页面上下文中使用（依赖特定的全局状态）
- 无特殊条件，可在任何需要数据查询的场景中使用
}}
