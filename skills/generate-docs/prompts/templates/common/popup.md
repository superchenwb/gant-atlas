# 弹窗 / 抽屉：{弹窗名称}

> 优先级说明：如果该弹窗由某个复杂按钮独占触发，优先把它并入对应 `button-{name}.md` 进行一体化描述。本模板只用于复用型弹窗或必须独立表达的弹窗。

---

## 一、弹窗概述

| 项目 | 说明 |
|------|------|
| 弹窗名称 | {名称} |
| 弹窗类型 | 抽屉 / 模态框 / 页签弹窗 / 选择弹窗 |
| 触发入口 | {页面按钮 / 行操作 / 单元格点击} |
| 打开条件 | {条件} |
| 弹窗目标 | {如：新增、编辑、关联、查看明细} |

---

## 二、内容结构

| 区域 | 内容 | 说明 |
|------|------|------|
| 基本字段区 | {字段说明} | |
| 子表格 / 子页签 | {如有则写} | |
| 底部按钮 | {保存、取消、确认等} | |

---

## 三、字段 / 列清单

### 3.1 表单字段

| 序号 | 字段标签 | 参数名 | 控件类型 | 码表/枚举编码 | 必填 | 默认值/回显规则 | 只读/禁用条件 | 说明 |
|------|----------|--------|----------|-------------|------|-----------------|---------------|------|
| 1 | {label} | `{fieldKey}` | {控件类型} | {码表编码；非码表类型写 -} | `是` / `否` | {规则} | {条件} | {说明} |

### 3.2 弹窗内表格（如有）

| 序号 | 列标题 | 字段名 | 展示类型 | 码表/枚举编码 | 是否可编辑 | 点击后行为 | 说明 |
|------|--------|--------|----------|-------------|------------|------------|------|
| 1 | {列标题} | `{field}` | {类型} | `是` / `否` | {行为} | {说明} |

---

## 四、联动与校验

### 4.1 字段联动

| 触发字段 | 受影响字段 | 触发时机 | 联动结果 | 说明 |
|----------|------------|----------|----------|------|
| {字段A} | {字段B} | 值变化时 | 清空 / 重载选项 / 自动赋值 / 校验 | {说明} |

### 4.2 校验规则

| 范围 | 规则类型 | 规则内容 | 不通过时结果 |
|------|----------|----------|--------------|
| 字段 / 表格行 / 整个弹窗 | 必填 / 格式 / 范围 / 条件校验 | {规则} | {提示并阻止提交等} |

---

## 五、提交规则

### 5.1 提交前处理

| 步骤 | 处理内容 | 转换前 | 转换后 |
|------|----------|--------|--------|
| 1 | {说明} | {原格式} | {提交格式} |

### 5.2 接口与提交结果

| 项目 | 说明 |
|------|------|
| 提交接口 | `{METHOD} {URL}` |
| 请求参数 | {说明} |
| 成功后结果 | {关闭弹窗 / 刷新当前列表 / 刷新详情 / 保持打开} |
| 失败后结果 | {提示、保留输入、定位错误字段等} |

---

## 六、特殊说明

- {如有不同模式下字段差异}
- {如有不同状态下提交限制}
