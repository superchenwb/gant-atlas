# 表单区域

---

## 一、表单概述

| 项目 | 说明 |
|------|------|
| 表单类型 | 编辑表单 / 展示表单 / 混合（部分可编辑） |
| 提交方式 | {提交方式说明} |
| 重置方式 | {按钮或交互说明} |
| 默认值来源 | {Props 传入 / 组件内初始值 / 无} |

> 本文件只负责表单区事实，不负责按钮功能主描述。

---

## 二、表单字段清单

| 序号 | 字段标签 | 参数名 | 控件类型 | 码表/枚举编码 | 必填 | 默认值 | 占位提示 | 只读/禁用条件 | 说明 |
|------|----------|--------|----------|-------------|------|--------|----------|---------------|------|
| 1 | {label} | `{fieldKey}` | {控件类型} | {码表编码，如 COMMON_EFFECT_STATUS；非码表类型写 -} | `是` / `否` | {默认值} | {placeholder} | {条件} | {说明} |

控件类型填写规则：

- 源码为 `Select` 时，固定写"下拉选择器"。
- 源码为 `TreeSelect` 时，固定写"树选择器"。
- 源码为 `LoupeSelect` 且明确配置 `hasOpen: true` 时，固定写"放大镜选择器（支持下拉选择和放大镜弹窗选择）"。
- 源码为 `LoupeSelect` 且未配置 `hasOpen: true` 时，固定写"放大镜选择器（仅支持放大镜弹窗选择）"。
- 源码为 `InputNumber` 时，固定写"数字输入框"。
- 源码为 `DatePicker` 时，固定写"日期选择器"。
- 源码为自定义业务放大镜组件时，写"放大镜选择器（下拉能力待确认，支持放大镜弹窗选择）"。
- 控件类型为"下拉选择器"或"编码下拉"时，码表/枚举编码列必填。

要求：
- 每一个字段单独一行
- 有联动、校验、值变换规则的字段，在后文单独展开

---

## 三、字段联动

| 触发字段 | 受影响字段 | 触发时机 | 联动结果 | 说明 |
|----------|------------|----------|----------|------|
| {字段A} | {字段B} | 值变化时 | 清空 / 回填 / 重载选项 / 自动赋值 / 改变可编辑状态 | {说明} |

---

## 四、校验规则

| 范围 | 规则类型 | 规则内容 | 不通过时结果 |
|------|----------|----------|--------------|
| {单个字段/多个字段/整表} | {必填/格式/范围/条件校验} | {规则} | {结果} |

---

## 五、值处理与转换

| 字段标签 | 处理时机 | 处理内容 | 转换前 | 转换后 |
|----------|----------|----------|--------|--------|
| {字段标签} | {初始化/提交前/依赖变化} | {说明} | {原格式} | {目标格式} |

---

## 六、数据加载与刷新

| 场景 | 接口 | 说明 | 结果 |
|------|------|------|------|
| 表单初始化 | `{METHOD} {URL}` 或 Props 传入 | {请求参数说明} | {结果} |
| 依赖字段变化 | {数据来源接口或说明} | {说明} | {结果} |

---

## 七、引用的其他组件文档

| 组件 | 文档路径 | 引用位置 |
|------|----------|----------|
| {组件名} | {文档路径} | {在何处引用} |
