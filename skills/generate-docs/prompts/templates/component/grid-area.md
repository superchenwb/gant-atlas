# 表格区域

---

## 一、表格概述

| 项目 | 说明 |
|------|------|
| 行数据唯一标识 | `{rowkey 字段名，如 id / code}` |
| 行选择模式 | `无` / `单选` / `多选` |
| 是否支持行内编辑 | `是` / `否` |
| 是否支持树形结构 | `是` / `否` |
| 默认排序 / 分页 | {说明} |

> 本文件只负责表格区事实，不负责按钮功能主描述。

---

## 二、列清单

| 序号 | 列标题 | 字段名 | 展示类型 | 码表/枚举编码 | 是否可编辑 | 默认值/格式化规则 | 只读/禁用条件 | 说明 |
|------|--------|--------|----------|-------------|------------|-------------------|---------------|------|
| 1 | {列标题} | `{field}` | {文本/码表/日期/数字/链接/自定义} | {码表编码，如 COMMON_EFFECT_STATUS；非码表类型写 -} | `是`/`否` | {规则} | {条件} | {说明} |

要求：
- 每一列单独一行
- 有点击行为、格式化规则、编辑规则的列，在后文单独展开
- 展示类型为"码表"时，码表/枚举编码列必填

---

## 三、表格联动与校验

### 3.1 列联动

| 触发列 | 受影响列 | 触发时机 | 联动结果 | 说明 |
|--------|----------|----------|----------|------|
| {列A} | {列B} | {时机} | {清空/回填/重载/改变可编辑状态} | {说明} |

### 3.2 校验规则

| 范围 | 规则类型 | 规则内容 | 不通过时结果 |
|------|----------|----------|--------------|
| {单元格/整行/整表} | {必填/格式/范围/条件校验} | {规则} | {结果} |

---

## 四、操作列与点击行为

### 4.1 行操作

| 按钮名称 | 显示条件 | 禁用条件 | 点击结果 | 二次确认 | 对应文档 |
|----------|----------|----------|----------|----------|----------|
| {按钮名} | {条件} | {条件} | {结果} | {文案或无} | {文档链接} |

要求：
- 每一个行操作按钮单独一行

### 4.2 单元格点击行为

| 列标题 | 触发元素 | 触发条件 | 点击结果 | 说明 |
|--------|----------|----------|----------|------|
| {列标题} | {文本/链接/图标} | {条件} | {跳转/弹窗/刷新局部} | {说明} |

要求：
- 每一个可点击单元格行为单独一行

---

## 五、数据加载与刷新

| 场景 | 接口 | 说明 | 结果 |
|------|------|------|------|
| 表格加载 | `{METHOD} {URL}` 或 Props 传入 | {请求参数说明} | {结果} |
| 行内保存 | `{METHOD} {URL}` | {请求参数说明} | {结果} |

---

## 六、引用的其他组件文档

| 组件 | 文档路径 | 引用位置 |
|------|----------|----------|
| {组件名} | {文档路径} | {在何处引用} |
