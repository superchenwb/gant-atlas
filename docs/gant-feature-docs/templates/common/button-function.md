# 按钮功能：{按钮名称}

> 本模板用于复杂按钮功能文档。优先用于“按钮 + 点击后弹窗 / 多步骤流程”这类一体化功能描述。

---

## 一、功能概述

| 项目 | 说明 |
|------|------|
| 按钮名称 | {名称} |
| 所属页面 / 区域 | {页面级 / Tab / 头部按钮 / 子页签} |
| 按钮位置 | {顶部左侧 / 顶部右侧 / Tab 内 / 头部 / 其他} |
| 功能目标 | {如：新增、编辑、关联、导入、审批、发布、选择} |
| 是否打开弹窗 | `是` / `否` |
| 弹窗名称 | {如有则写} |

---

## 二、入口与状态规则

| 项目 | 说明 |
|------|------|
| 显示条件 | {条件} |
| 禁用条件 | {条件} |
| 前置校验 | {如：需选中一行、需在草稿状态、需有权限} |
| 二次确认 | {确认文案或无} |

### 2.1 状态差异矩阵

| 状态维度 | 状态值 | 按钮可见性 | 按钮可用性 | 点击后结果 |
|----------|--------|------------|------------|------------|
| 页面状态 / 权限状态 / 选中状态 / 数据状态 | {如：草稿 / 已发布 / 无权限 / 已选中 / 未选中} | {显示 / 隐藏} | {可用 / 禁用} | {结果} |

---

## 三、按钮点击后的流程

| 步骤 | 用户操作 | 页面结果 |
|------|----------|----------|
| 1 | 点击按钮 | {结果} |
| 2 | {如：打开弹窗 / 加载数据 / 弹出确认框} | {结果} |
| 3 | {如：填写字段 / 选择数据 / 点击底部按钮} | {结果} |
| 4 | {如：提交 / 关闭 / 返回} | {结果} |

---

## 四、弹窗或后续内容

### 4.1 弹窗概述

| 项目 | 说明 |
|------|------|
| 弹窗名称 | {名称} |
| 弹窗类型 | 抽屉 / 模态框 / 选择弹窗 / 其他 |
| 打开条件 | {条件} |
| 关闭方式 | {右上角关闭 / 取消 / Esc / 点击遮罩 / 其他} |

### 4.2 内容结构

| 区域 | 内容 | 说明 |
|------|------|------|
| 基本字段区 | {字段说明} | |
| 子表格 / 子页签 | {如有则写} | |
| 底部按钮 | {保存、取消、确认等} | |

### 4.3 字段 / 列清单

| 序号 | 标题/标签 | 参数名 | 控件类型 | 必填 | 默认值/回显规则 | 只读/禁用条件 | 说明 |
|------|-----------|--------|----------|------|-----------------|---------------|------|
| 1 | {标题或标签} | `{fieldKey}` | {控件类型} | `是` / `否` | {规则} | {条件} | {说明} |

要求：

- 每一个字段、每一列都单独一行。
- 如果存在弹窗内表格，列、点击行为、行操作也要逐项列出。

### 4.4 联动与校验

| 触发字段/列 | 受影响字段/列 | 触发时机 | 联动结果 | 说明 |
|-------------|---------------|----------|----------|------|
| {字段A} | {字段B} | {时机} | {结果} | {说明} |

| 范围 | 规则类型 | 规则内容 | 不通过时结果 |
|------|----------|----------|--------------|
| 字段 / 表格行 / 整个弹窗 | 必填 / 格式 / 范围 / 条件校验 | {规则} | {结果} |

### 4.5 底部按钮与提交

| 按钮名称 | 显示条件 | 点击结果 | 二次确认 | 说明 |
|----------|----------|----------|----------|------|
| {如：保存 / 确认 / 提交 / 取消} | {条件} | {结果} | {确认文案或无} | {说明} |

---

## 五、提交与结果

### 5.1 提交前处理

| 步骤 | 处理内容 | 转换前 | 转换后 |
|------|----------|--------|--------|
| 1 | {说明} | {原格式} | {提交格式} |

### 5.2 接口与结果

| 项目 | 说明 |
|------|------|
| 提交接口 | `{METHOD} {URL}` |
| 请求参数 | {说明} |
| 成功后结果 | {关闭弹窗 / 刷新列表 / 刷新详情 / 刷新当前区域 / 保持打开} |
| 失败后结果 | {提示、保留输入、定位字段等} |

---

## 六、关联文档

| 文档 | 用途 |
|------|------|
| [button-area.md](./button-area.md) | 按钮总清单 |
| [grid-area.md](./grid-area.md) / 其他区域文档 | 相关主内容区 |
| [popup-{name}.md](./popup-{name}.md) | 仅当该弹窗需独立表达时保留 |

---

## 七、特殊说明

- {如不同状态下弹窗字段差异}
- {如功能依赖页面或详情状态}
- {如该按钮功能会影响当前 Tab、当前列表、整个页面或后续流程}
