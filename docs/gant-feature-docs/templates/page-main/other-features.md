# 其他可见模块

---

## 一、模块清单

| 模块名称 | 位置 | 展示内容 | 触发条件 | 对用户的作用 |
|----------|------|----------|----------|--------------|
| {模块名称} | {位置} | {内容} | {条件} | {作用} |

要求：

- 页面上每一个独立可见模块都要单独列出。
- 不能只写“有其他模块”。
- 如果某个模块内部包含多个入口或多个局部功能，要继续拆成模块内功能清单。

---

## 二、独立交互说明

| 模块名称 | 用户操作 | 页面结果 | 说明 |
|----------|----------|----------|------|
| {模块名称} | {操作} | {结果} | {说明} |

要求：

- 一个模块如果有多个独立操作，要拆成多行。
- 如果模块包含入口按钮、切换项、展开收起、局部刷新等，也要逐项描述。

---

## 三、模块内功能索引

| 模块名称 | 子功能名称 | 触发入口 | 关键条件 | 页面结果 |
|----------|------------|----------|----------|----------|
| {模块名称} | {如：展开树节点 / 上传附件 / 切换视图} | {入口} | {条件} | {结果} |

要求：

- 模块内每一个独立功能都要单独列出。
- 不允许只写模块级概述。

---

## 四、状态差异矩阵

| 模块名称 | 状态条件 | 可见内容 / 可用功能 | 页面结果 |
|----------|----------|---------------------|----------|
| {模块名称} | {如：空态 / 非空态 / 有权限 / 无权限 / 展开 / 收起} | {结果} | {结果} |

---

## 五、接口与数据来源

| 模块名称 | 接口 / 数据来源 | 说明 |
|----------|-----------------|------|
| {模块名称} | `{METHOD} {URL}` / 本地数据 / 继承主查询结果 | {说明} |

---

## 六、特殊说明

- {如该模块仅在特定状态可见}
- {如该模块与主表格联动}
