# 表格区域

---

## 一、表格概述

| 项目 | 说明 |
|------|------|
| 表格名称 | {如有} |
| 行选择模式 | `无` / `单选` / `多选` |
| 是否支持行内编辑 | `是` / `否` |
| 是否支持树形结构 | `是` / `否` |
| 默认排序 / 分页 | {说明} |

> 不能只写表格概述。表格里的每一个具体功能都必须逐项落文档。
>
> 本文件只负责表格 / 主内容区事实，不负责按钮功能主描述。

---

## 二、列清单

| 序号 | 列标题 | 字段名 | 展示类型 | 是否可编辑 | 默认值/格式化规则 | 只读/禁用条件 | 说明 |
|------|--------|--------|----------|------------|-------------------|---------------|------|
| 1 | {列标题} | `{field}` | {文本 / 码表 / 日期 / 数字 / 链接 / 自定义} | `是` / `否` | {规则} | {条件} | {说明} |

要求：

- 每一列单独一行。
- 有点击行为、格式化规则、编辑规则的列，不能只写在“说明”里一笔带过，必须在后文单独展开。

---

## 三、表格联动与校验

### 3.1 列联动

| 触发列 | 受影响列 | 触发时机 | 联动结果 | 说明 |
|--------|----------|----------|----------|------|
| {列A} | {列B} | 编辑或点击后 | 清空 / 回填 / 重载 / 改变可编辑状态 | {说明} |

### 3.2 校验规则

| 范围 | 规则类型 | 规则内容 | 不通过时结果 |
|------|----------|----------|--------------|
| 单元格 / 整行 / 整表 | 必填 / 格式 / 范围 / 条件校验 | {规则} | {结果} |

---

## 四、操作列与点击行为

### 4.1 行操作

| 按钮名称 | 显示条件 | 禁用条件 | 点击结果 | 二次确认 | 接口 / 文档 |
|----------|----------|----------|----------|----------|-------------|
| {按钮名称} | {条件} | {条件} | {结果} | {确认文案或无} | {接口或文档链接} |

要求：

- 每一个行操作按钮单独一行。
- 不能合并写“操作列支持编辑、删除、查看”。

### 4.2 单元格点击行为

| 列标题 | 触发元素 | 触发条件 | 点击结果 | 说明 |
|--------|----------|----------|----------|------|
| {列标题} | {文本 / 链接 / 图标} | {条件} | {跳转 / 弹窗 / 刷新局部} | {说明} |

要求：

- 每一个可点击单元格行为单独一行。
- 不能只写“某列可点击”，必须写点击后跳转、打开弹窗还是刷新局部。

---

## 五、数据加载与提交

| 场景 | 接口 | 结果 |
|------|------|------|
| 表格加载 | `{METHOD} {URL}` | {结果} |
| 行内保存 | `{METHOD} {URL}` | {结果} |
| 行删除 / 状态变更 | `{METHOD} {URL}` | {结果} |

---

## 六、特殊说明

- {如树形展开规则}
- {如多表头或动态列}
