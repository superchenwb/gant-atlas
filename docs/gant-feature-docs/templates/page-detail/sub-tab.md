# 子页签：{页签名称}

---

## 一、页签概述

| 项目 | 说明 |
|------|------|
| 页签名称 | {名称} |
| 展示条件 | {条件} |
| 默认是否加载 | `是` / `否` |
| 主要内容 | {表格 / 表单 / 混合区域} |

> 不能只写子页签概述。子页签中的每一个具体功能都必须逐项描述完整。

---

## 二、页签结构

| 区域 | 内容 | 对应文档 |
|------|------|----------|
| 按钮区域 | {说明} | [button-area.md](./button-area.md) / 当前文档 |
| 主表格 / 主内容区 | {说明} | [grid-area.md](./grid-area.md) / 当前文档 |
| 弹窗 | {说明} | [popup-{name}.md](./popup-{name}.md) |

要求：

- 如果当前页签同时存在按钮区和表格区，必须拆成独立 `button-area.md` 和 `grid-area.md`。
- 不要把按钮详细功能混写进 `grid-area.md`。
- 不要把表格列和行操作混写进 `button-area.md`。
- 弹窗较复杂时必须单独生成 `popup-{name}.md`。

---

## 三、按钮区域

| 按钮名称 | 显示条件 | 点击结果 | 二次确认 | 接口 / 文档 |
|----------|----------|----------|----------|-------------|
| {按钮名称} | {条件} | {结果} | {确认文案或无} | {接口或文档} |

要求：

- 每一个按钮单独一行。
- 不能合并写“支持新增、编辑、删除”。
- 如果按钮打开弹窗，而弹窗内容不单独建文件，也必须在这里完整写出弹窗字段、联动、校验、提交结果。
- 如果按钮打开的弹窗内容较大、字段较多、流程较复杂，必须拆成单独弹窗文档，不得只写一句“打开弹窗”。

---

## 四、表格 / 内容区

### 4.1 列或字段清单

| 序号 | 标题/标签 | 字段名 | 展示类型 | 是否可编辑 | 说明 |
|------|-----------|--------|----------|------------|------|
| 1 | {标题或标签} | `{field}` | {类型} | `是` / `否` | {说明} |

要求：

- 每一列、每一个字段都单独一行。
- 有点击行为、编辑规则、联动规则的项，必须在后文继续展开。

### 4.2 联动与校验

| 触发字段/列 | 受影响字段/列 | 触发时机 | 联动结果 | 说明 |
|-------------|---------------|----------|----------|------|
| {字段A} | {字段B} | {时机} | {结果} | {说明} |

### 4.3 刷新与结果

| 场景 | 页面结果 |
|------|----------|
| 进入页签 | {结果} |
| 查询或刷新 | {结果} |
| 保存或操作成功 | {结果} |

### 4.4 行操作与点击行为（如有）

| 功能名称 | 触发入口 | 前置条件 | 页面结果 | 接口 / 文档 |
|----------|----------|----------|----------|-------------|
| {如：编辑行 / 删除行 / 查看明细 / 点击编码跳转} | {按钮 / 单元格 / 图标} | {条件} | {结果} | {接口或文档} |

要求：

- 每一个行操作、每一个点击行为都单独一行。
- 不能只在“说明”里概括带过。

---

## 五、关联弹窗

| 弹窗名称 | 触发入口 | 用途 | 详细文档 |
|----------|----------|------|----------|
| {弹窗名称} | {入口} | {用途} | [popup-{name}.md](./popup-{name}.md) |

要求：

- 每一个弹窗都要说明对应触发按钮。
- 弹窗文档里要完整覆盖字段、联动、校验、提交和结果。

---

## 六、接口导航

| 场景 | 接口 | 说明 |
|------|------|------|
| 子页签加载 / 查询 | `{METHOD} {URL}` | {说明} |
| 子页签保存 / 操作 | `{METHOD} {URL}` | {说明} |

---

## 七、特殊说明

- {如子页签仅在特定详情状态显示}
- {如子页签依赖头部字段变化}
