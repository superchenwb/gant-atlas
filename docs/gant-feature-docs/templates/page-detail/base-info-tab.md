# 基本信息页签

---

## 一、页签概述

| 项目 | 说明 |
|------|------|
| 页签名称 | {名称} |
| 展示条件 | {条件} |
| 是否默认展示 | `是` / `否` |

---

## 二、字段清单

| 序号 | 字段标签 | 参数名 | 控件类型 | 必填 | 默认值/回显规则 | 只读/禁用条件 | 说明 |
|------|----------|--------|----------|------|-----------------|---------------|------|
| 1 | {label} | `{fieldKey}` | {控件类型} | `是` / `否` | {规则} | {条件} | {说明} |

---

## 三、联动与校验

### 3.1 字段联动

| 触发字段 | 受影响字段 | 触发时机 | 联动结果 | 说明 |
|----------|------------|----------|----------|------|
| {字段A} | {字段B} | 值变化时 | 清空 / 回填 / 重载 / 改变可编辑状态 | {说明} |

### 3.2 校验规则

| 字段标签 | 规则类型 | 规则内容 | 触发条件 |
|----------|----------|----------|----------|
| {字段标签} | 必填 / 格式 / 范围 / 条件校验 | {内容} | {条件} |

---

## 四、保存与结果

| 项目 | 说明 |
|------|------|
| 保存接口 | `{METHOD} {URL}` |
| 提交参数 | {说明} |
| 成功后结果 | {提示、刷新头部、刷新当前页签等} |
| 失败后结果 | {提示、保留编辑态、定位字段等} |

---

## 五、关联弹窗（如有）

| 弹窗名称 | 触发入口 | 作用 | 详细文档 |
|----------|----------|------|----------|
| {弹窗名称} | {入口} | {作用} | [popup-{name}.md](./popup-{name}.md) |

---

## 六、特殊说明

- {如不同详情状态下字段可编辑性不同}
- {如字段分组展示}
