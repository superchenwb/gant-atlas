# {{componentName}}

> 所属业务领域：{{businessDomain}}
> 组件形态：{{componentForm}}（如：弹窗选择器 / 内嵌表格 / 按钮组 / 复合面板）

## 概述

{{一句话说明组件的核心用途，例如："用于在 BOM 主页面选择目标产品，支持下拉选择和放大镜弹窗两种选择方式。"}}

---

## Props 接口

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| {{prop1}} | {{类型描述}} | 是 | - | {{说明}} |
| {{prop2}} | {{类型描述}} | 否 | {{默认值}} | {{说明}} |

<!--
类型描述示例：
- 字符串
- 数字
- 布尔值
- 对象（{id: 字符串, name: 字符串}）
- 字符串数组
- 函数（参数说明 => 返回值说明）
-->

---

## 交互行为

### {{交互名称1，如：下拉选择}}

- **触发方式**：{{用户如何触发，如"点击下拉框，从选项列表中选择"}}
- **组件响应**：{{组件发生什么变化，如"选中项高亮显示，输入框显示选中值"}}
- **回调触发**：{{是否触发外部回调，如"触发 onChange，传出选中项的 id 和 name"；若无则写"无"}}

### {{交互名称2，如：放大镜弹窗选择}}

- **触发方式**：{{触发方式描述}}
- **组件响应**：{{响应描述}}
- **回调触发**：{{回调描述}}

### {{交互名称3，如：清空}}

- **触发方式**：{{触发方式描述}}
- **组件响应**：{{响应描述}}
- **回调触发**：{{回调描述}}

<!--
常见交互类型：
- 选择/确认
- 取消/关闭
- 清空/重置
- 搜索/过滤
- 展开/收起
- 翻页
- 行操作（编辑/删除）
-->

---

## 数据流与逻辑

- **数据加载时机**：{{何时发起数据请求，如"组件挂载时自动加载"、"用户输入搜索词后触发"、"传入的 xxx 参数变化时重新加载"}}
- **加载失败处理**：{{加载失败时组件呈现何种状态，如"显示空状态，不展示错误提示"}}
- **可见状态**：
  - 加载中：{{加载中的展示形式，如"显示加载动画，禁用操作"}}
  - 禁用：{{禁用条件和展示形式，如"传入 disabled=true 时，下拉和按钮均不可操作"}}
  - 空态：{{无数据时的展示形式，如"显示'暂无数据'"}}
  - {{其他状态}}：{{描述}}
- **关键时机**：
  - 初始化：{{组件值如何初始化，如"由 value 参数控制初始值"}}
  - 重置：{{何时重置，如"传入的 resetKey 变化时清空已选值"}}
  - 依赖变化：{{哪些参数变化会触发重新加载或重置，如"bomType 参数变化时重新加载选项列表"}}

---

## 使用场景

{{描述适合使用该组件的页面类型或功能场景，例如：
- 用于 BOM 主页面的产品筛选查询
- 用于变更单表单中选择关联零件
}}

**前提条件**：

{{描述使用该组件的前提，例如：
- 需要在变更单详情页上下文中使用（依赖页面提供的权限信息）
- 无特殊前提，可在任意表单中使用
}}

**配合使用注意事项**：

{{与其他组件或数据源配合使用时的注意点，例如：
- 与 SearchForm 配合使用时，需将组件返回的 id 传入搜索参数
- 与 Grid 配合使用时，选中值会联动过滤表格数据
- 无特殊注意事项
}}
